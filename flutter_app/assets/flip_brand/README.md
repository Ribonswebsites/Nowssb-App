# Flip brand assets

Nine brand stills (`img1.jpg` … `img9.jpg`) for `FlipBrandShowcase`. PNG payloads with `.jpg` names — **do not recompress**.

## Status

- Layout + Normal-home wiring (Flip = first H-scroll card) is on `main`.
- Binaries are in local commit `8e4eea7` but not on remote yet: no `git push` credentials here; ~13MB is too large for Contents-API text uploads.

## Finish

```bash
cd /workspace/Nowssb-App
git push origin 8e4eea7:main
# or copy flutter_app/assets/flip_brand/img*.jpg from that commit and push
```
