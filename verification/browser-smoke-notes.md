Browser smoke test checkpoint (Aug 23, 2026): local native bundle built successfully with `npm run build:native` (263 files, 158.7 MB). Initial browser navigation loaded the app shell and exposed existing hidden screen text; the follow-up browser view unexpectedly landed on about:blank with no rendered elements, so a second browser pass is required for reliable DOM-order verification. No claim of visual verification is made yet.
Second browser checkpoint: the local WebView bundle extracted the new `Your essentials` section with Recents/Favorites controls and four NowssB rows below the dashboard. Console DOM inspection confirmed Normal children order: greeting → search → `.nwsb-daydash-normal` → `.nwsb-essentials-normal`; Fashion order: `home-greeting` → `home-tagline` → `.nwsb-daydash-fashion` → `.nwsb-essentials-fashion`. The live dashboard selected the Evening slot at test time. The first pass currently labels one web row `Meaning Store`; source should be aligned to Flutter as `Word Science` with the verified `word-science` destination before final commit.
