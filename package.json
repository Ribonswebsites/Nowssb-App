{
  "name": "nowssb-app",
  "version": "9.5.0",
  "private": true,
  "description": "NowssB — Shabdapathy. The web app, and the Capacitor shell that puts it on Google Play.",
  "type": "module",
  "scripts": {
    "build:native": "node tools/build-native.mjs",
    "android:add": "npm run build:native && npx cap add android",
    "android:sync": "npm run build:native && npx cap sync android",
    "android:open": "npx cap open android",
    "android:run": "npm run build:native && npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.0",
    "@capacitor/push-notifications": "^8.1.2",
    "@capacitor/splash-screen": "^8.0.2",
    "@capacitor/status-bar": "^8.0.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
